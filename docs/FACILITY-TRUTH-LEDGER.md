# Facility-Truth Ledger

Generated from the 718-endpoint audit (2026-07-03). Three sections:
what is COMPANY-WIDE BY DESIGN (never facility-filtered, on purpose), what
still LACKS FACILITY LINKAGE (schema work queued), and what was DEFERRED
from the first batch fix with the reason.

## Deferred from the first batch (do next)
- /api/referrals family — the endpoint already uses a `facility` query param
  for PARTNER facilities (different table); scoping via the chip param would
  collide. Needs a param rename or origin_facility_id column first.
- Census email fan-out (/api/command/census/email + auto-send) — it is the
  Akron detox daily report today; fan out per facility (like billing
  readiness) when Wheatfield goes live.
- sl-kiosk (housing resident kiosk) — lists all sober-living residents; only
  Hilltop Akron has residents today. Scope when per-facility housing kiosk
  codes ship.
- Staffing by-id write guards (delete slot/assign/calloff/cover/attendance) —
  resolve the row's facility through slot_id and require facilityAllowed.
- getOrCreateShift UNIQUE(date,name) → UNIQUE(date,name,facility_id) migration
  (legacy lineup world).
- schedule_items.facility_id (program schedule), beds.facility_id (bed board
  inventory), shift_lead per facility.

## Company-wide by design (no action)
- POST /api/arrivals/sync — Recorded as a deliberate design: the Salesforce pipeline is treated as the detox front door, so every synced arrival is stamped with defaultFacilityId(). Correc
- GET /api/referrals/meta — Static vocabulary (departments, categories, reasons, facility types, SF flag). No facility data.
- GET /api/facilities + POST /api/facilities + POST /api/facilities/:id/preferred + POST /api/partners/approve — The partner directory (facilities table, src/db.js:774) is the company-wide list of external referral partners — a different concept from the 11 org_facilities,
- POST /api/debrief-discharges + GET /api/debrief-discharges/status — Admin-only AI batch job; runDischargeDebriefs (3804-3806) selects every kipu discharge in 21 days org-wide and writes back to each client row (facility preserve
- POST /api/command/discharge-cleanup — Admin maintenance: deletes orphan/stale discharge flow_events org-wide and re-rolls today's metrics. Data-hygiene job, intentionally company-wide.
- POST /api/kipu/fix-discharge-dates — One-time admin repair job for backfilled discharge dates; operates on the whole Kipu-synced dataset by design.
- GET /api/arrivals/diagnose — Admin lookup of a single Salesforce Lead by name (sfArrivalsDiagnose); returns SF data, touches no local facility-linked rows.
- GET /api/arrival/template + POST /api/arrival/template — arrival_items is the single org-wide on-arrival checklist template (no facility_id column). Recorded as company-wide standards content; would become per-facilit
- GET /api/surveys — Survey definitions/questions are shared company content (like training and the handbook) — the same instruments run at every facility. Only responses need facil
- GET+POST /api/checkins/questions — The rotating shift question asked on rounds is company-wide culture content (single global list). Recording as intentional; if the owner ever wants per-building
- POST /api/auth-register/run-reminders (+ auto sweep) — UR renewal watch deliberately sweeps all facilities into one email to authRecipients() (state auth_email, falling back to insuranceRecipients). Central revenue-
- POST /api/auth-register/settings — Sets the single org-wide auth_email state key (UR watch recipients). Company-wide config by design.
- POST /api/billingready/settings — Note-type mapping / LOC rulebook / check hour stored in one global billing_ready_cfg state key — the payor rulebook is company-wide by design. Per-facility emai
- GET /api/appts/availability — A staff member's working hours, blocks and existing bookings are per-PERSON; the cross-facility appointment lookup in staffDay (server.js:1100) is intentional —
- GET/POST /api/appts/staffsetup — Weekly hours + committed blocks keyed by staff_name only (src/db.js:2235-2251). Person-level org data; self-or-leadership edit rule enforced at server.js:1130.
- POST /api/finance/rates, POST /api/finance/recompute — loc_rates (per-ASAM per-diem) is one global state key and recompute rebuilds the whole org ledger. Rates are treated as company-wide pricing by design today; no
- POST /api/finance/expenses-save, /burden, /salaried, /staffing-rate, /shift-hours, /role-rates, /staff-rates — All seven finance writes mutate global state keys (expense_cats, ppd_lines, budget_monthly, expense_actuals, payroll_burden, salaried_roles, shift_hours, role_r
- GET/POST/DELETE /api/corp/insurance (+ /brokers, /settings, /run-reminders, /extract, /api/corp/files/:id) — Corporate insurance program (GL/professional/property policies, brokers, coverage matrix). insurance_policies has no facility_id — its 'entity' column is a free
- GET/POST /api/command/census/recipients — Reads/writes the single census_email_to state key — global recipient config by design today (would become per-facility via facSetting 'report_email' if the cens
- insuranceRecipients() fallback chain — insuranceRecipients() = insurance_email state, else hardcoded chavaa@armadarecovery.com + census_email_to. Org-wide by design for corp-insurance reminders, but 
- GET/POST /api/clock/geofence, POST /api/clock/allow-network, POST /api/clock/clear-networks — Legacy org-wide fence + approved-WiFi IP list kept as global config; per-facility fences already exist separately via facSettings/facilityFences() (server.js:10
- GET /api/staff-activity (staffsignins view) — Staff accounts and last sign-in are org-level records (users table has no facility dimension by design; admin-only). The 'staffsignins' view (public/app.js:5913
- GET /api/kipu/coverage — Despite the 'coverage' name this is an admin-only Kipu data-health diagnostic (which client fields are populated), not staffing coverage. Company-wide integrati
- POST /api/finance/shift-hours — Org-global config knob (admin-only) in the finance world; recorded because it is named 'shift-hours'. Becomes per-facility only if payroll modeling ever splits 
- GET /api/today — The Today inbox is 'everything waiting on ME' — every query is keyed to req.user (assignee/owner/name), which is the correct boundary; classifying org-level-by-
- GET/POST/PATCH/DELETE /api/desk* (My Desk) — Decision: My Desk is the owner's personal capture board — the correct boundary is owner_id=req.user.id, which every query enforces. desk_items.facility_id (src/
- POST /api/command/discharge-cleanup — Admin data-hygiene action that deletes orphan/stale flow events across the whole org and re-rolls today's metrics. It exposes no data and the invariants it repa
- GET/POST /api/command/census/recipients, POST /api/command/meals/recipients, POST /api/desk/settings — Email-recipient and digest configuration stored in single org-wide app_state keys. Company-wide by design today; note per-facility emails would eventually need 
- GET /api/team — The recognition wall + daily-training ack + admin pulse trend. kudos, training_ack, staff_pulses have no facility columns (src/db.js:485-497, 313-320) — recogni
- GET /api/team-stats (6956), GET /api/user-stats/:id (6948), GET /api/my-stats (6927) — Per-person duty-performance stats computed by userStats() (line 6880) from the caller's own rows in time_entries/round_scans/schedule_assignments etc. The team 
- GET /api/messages/threads (11532), GET /api/messages (11552), POST /api/messages (11561), GET /api/messages/unread (11573), GET /api/staff (11515) — Staff chat: one company-wide 'team' channel plus per-pair DMs (messages table keyed by channel, src/db.js:636-648). Mission explicitly lists staff messages as o
- POST /api/kudos (11727), POST /api/kudos/extract (11735), POST /api/kudos/bulk (11744), GET /api/extra-mile (11770), POST /api/extra-mile (11773) — Recognition capture and the Extra Mile wall. kudos and extra_mile (src/db.js:249-257, 485-491) have no facility columns; recognition is one org-wide program fee
- GET /api/staff-voice (11597), POST /api/staff-voice (11601), POST /api/staff-voice/:id/respond (11609) — Anonymous-capable staff feedback loop (staff_voice, src/db.js:225-236, no facility column). Anonymity is a feature; stamping a facility on an 'anonymous' post f
- GET /api/workplace (11670), GET /api/workplace/attention (11664) — The morale dashboard (the closest thing to /api/morale). Aggregates staff_pulses, kudos, wows, staff_voice, excellence_surveys, hr_coaching, weekly_reflections 
- POST /api/staff-pulse (8603), POST /api/belonging-pulse (6281), GET /api/belonging-pulse/stats (6291) — Wellbeing pulses. staff_pulses is per-user with no facility; belonging_pulses stores NO user at all — deliberately anonymous (src/db.js:269-275, comment 'no use
- GET /api/lineup/email-preview (8369), POST /api/lineup/send (8400) — The 8am lineup is ONE company email to one recipient list (lineup_email app_state), built by buildLineupEmail() with censusNow() and lineupWins() (line 8140+) c
- Lineup state singletons: GET/POST /api/lineup/spotlight (8429/8434), POST /api/settings/lineup-email (8442), POST /api/lineup/value/set (8497), GET /api/principle/today (8472), POST /api/principle/set (8488), POST /api/principle/story (8588) — All read/write app_state singletons (today's spotlight, service value, principle override, email settings) — one value for the whole company, matching the singl
- Raffle: GET /api/lineup/raffle (8516), POST entry (8523), DELETE entry/:id (8532), POST draw (8538), POST set-winner (8551), POST clear-winner (8559), GET winner (8565) — One weekly company recognition raffle. raffle_entries (src/db.js:201-208) and the wows-derived ticket pool have no facility columns; the winner is a single app_
- GET /api/lineup/recognition (8570), DELETE /api/lineup/recognition/:type/:id (8582) — Curates kudos + extra_mile rows for the next company-wide lineup email; both tables are facility-less by design (see kudos/extra-mile finding).
- GET /api/handbook (11788), POST /api/reflection (11802), GET /api/reflections (11810) — Handbook is static content from src/handbook.js (principles, chapters, rotation) — explicitly org-level per the mission. Weekly reflections are per-user (weekly
- GET /api/exsurvey (11826), POST /api/exsurvey (11830), GET /api/exsurvey/results (11843) — Monthly Excellence Survey is stored WITHOUT the person's identity (role only; done-marker in a separate table so answers can never be joined to a person — src/d
- Training: POST /api/training-ack (11855), GET /api/courses (12519), GET /api/courses/:id (12524), POST /api/courses/:id/complete (12531), POST /api/courses (12544), GET /api/training-status (12552) — Training content (courses/course_questions) and per-user completions/acks — explicitly org-level per the mission (training content). training-status lists the o
- SOP/policy library: GET /api/docs (12487), POST /api/docs (12498), DELETE /api/docs/:id (12506), POST /api/docs/:id/read (12507) — This is the 'library' — the docs table (src/db.js:500-504) is company SOPs/policies with per-user read receipts. No facility column, and standards are intention
- Growth: GET /api/growth (11712), POST /api/growth (11717), GET/POST /api/growth/me (7348/7351), POST /api/growth/me/checkin (7352), GET /api/growth/:id (7357), POST /api/growth/:id/checkin (7362) — Growth plans and check-ins are keyed to users (growth_plans/growth_checkins, src/db.js:1453-1466 and 238-246) — per-person development records that follow the e
- Users admin: GET /api/users (6762), POST /api/users (6804), POST /api/users/:id/reinvite (6820), POST /api/users/:id (7432), DELETE /api/users/:id (7460) — Explicitly org-level per the mission. users has no facility column; facility membership is modeled separately in user_facility_access (src/db.js:2052-2057) and 
- POST /api/excellence/config (6348), POST /api/excellence/defect (6357) — config writes a single app_state JSON (excellence_cfg) — one empowerment/non-negotiables standard for the whole company, by design (a per-facility config would 
- My HR self-service: GET /api/myhr (6033), POST /api/myhr/leave (6046), POST /api/myhr/pulse (6057) — Strictly the caller's own record (employeeForUser matches by email/name, line 6023), own leave, own monthly eNPS pulse (hr_pulse UNIQUE(user_id,month)). Persona
- HR policy docs: GET /api/hcos/docs (5984), POST /api/hcos/docs (5989), DELETE /api/hcos/docs/:id (5996), POST /api/docs/:id/ack (5997) — Company policy documents (hr_documents) with per-user acknowledgements (hr_doc_acks keyed by user_id) — policies apply org-wide; the ack-count denominator (line
- Employee Care Card: GET /api/employee/:id/profile (7047), POST bigfive (7057), sjt (7081), disc (7101), profile (7116), note (7125), coach (7131) — Per-person leadership profiles (employee_profiles/employee_notes keyed by user_id, src/db.js:1438-1446) gated by canSeeTeamStats. A person's DISC/Big-Five/SJT a
- Leadership Mirror: GET /api/leadership/mirror/:id (7249), GET /api/leadership/leaders (7263), POST style (7269), POST judgment (7286), POST coach (7299) — CEO/leader development profiles stored in employee_profiles.leadership per user — org-level leadership program, same rationale as the Care Card.
- Daily focus: GET /api/focus (12566), POST /api/focus (12572), POST /api/focus/set (12577), GET /api/focus/history (12582) — One rotating daily focus topic for the whole company (app_state 'focus:<date>'), with per-user participation logs (focus_logs, no facility column, src/db.js:534
- Assigned-task thread: GET /api/assigned-tasks/:id (7648), POST comment (7657), POST done (7667), POST /api/assigned-tasks (7640) — Single-record endpoints guarded by participant check (assignee/assigner/admin, line 7651) — access is person-scoped, so the facility chip does not apply. The wr
- GET /api/invite/:token + POST /api/signup — Recorded as company-wide by design: users are an org-level table; the public invite lookup (server.js:6831) returns only {name,email} gated behind a 256-bit ran
- GET/POST /api/housing/kiosk-code — One kiosk code for the whole company, stored in a single state key 'sl_kiosk_code' (slKioskCode(), line 785). Admin/ED-gated. Recording as org-level, with the c
- GET /api/housing/recognition (+ POST, POST :id/kudos; staffhub feed at 2430) — Staff-to-staff 'wow story' recognition feed; housing_recognition keys to users (from_user, to_name, to_user_id) with no resident/house linkage. Staff culture co
- GET /api/housing/staff-dev, GET /api/housing/staff-dev/:id, POST :id/profile, POST :id/onboard, POST :id/competency — Staff profiles, sacred-orientation checklists, and competency sign-offs keyed by users.user_id (housing_staff_profile / housing_staff_onboard / housing_competen
- GET /api/housing/meta — Static reference constants (RECCAP_DOMAINS, PHASES, LOC, ORH_STANDARDS). No data rows. Company-wide by nature.
- GET /api/housing/forms/templates — Static FORM_TEMPLATES intake-packet definitions (module constant, line 90). The filled forms themselves live in housing_forms keyed by resident_id and are read 
- GET /api/housing/restrictions/meta — Static RESTRICTION_TYPES list. Company-wide by nature.
- GET /api/housing/activities/catalog (+ POST) — Curated org-wide activity library (housing_activity_catalog has no house/facility column); the scheduled EVENTS built from it carry house_id and are scoped. A s
- GET/POST /api/housing/targets — Best-in-class targets stored in one org-wide state key 'hilltop_targets' (getTargets, line 1090). Same numbers shown under every facility pick. Fine as company-
- POST /api/housing/standards (+ getStandards consumed by lineup/staffhub) — Service standards of the day stored in the single org-wide state key 'hilltop_standards' (2589). Culture/ritual content shared across facilities — consistent wi
- POST /api/housing/daily-movement/send (+ /settings, + auto-scheduler) — The emailed Daily Movement digest is deliberately org-wide: deliverDailyMovement (1259) calls buildDailyMovement(date) with fid=null, and the code comment at 11
- GET /api/org/facilities — The registry itself is org-wide by design (dropdowns need it), BUT this is the slice's read leak: guarded by requireAuth only (any logged-in user, no user_facil
- GET+POST /api/org/permissions — Role→module permission matrix (role_permissions), admin-only both directions. Genuinely company-wide configuration; no facility dimension exists or is needed (s
- POST /api/org/facilities, POST /api/org/facilities/:id, GET /api/org/module-catalog — Facility onboarding/edit and the module catalog (lines 1679, 1817, 1700) — admin-only registry writes; company-wide by nature.
- GET+POST /api/org/facilities/:id/settings — Per-facility settings are path-scoped by :id and admin-only (admins may see all facilities per facilityAllowed) — correct as is.
- GET+POST /api/org/facilities/:id/integrations, POST /api/org/facilities/:id/kipu-test, POST /api/org/facilities/:id/kipu-sync — Admin-only, path-scoped by facility id. Read returns booleans/location only (secrets write-only, line 1738-1747); kipu-sync (1797) stamps every synced row with 
- GET+POST /api/org/facility-access — The user↔facility access matrix itself — admin-only, inherently org-wide; POST validates user and facility ids and rewrites rows transactionally.
- GET /api/ownership — Portfolio census/admits/discharges roll-up across ALL facilities — cross-facility is the entire point of the view, so no ?facility= handling is expected. Caveat
- GET /api/corp/vendors — Corporate vendor rolodex ("who corporate calls"), served whole to requireCorp roles which are org-wide by definition. vendors.facility TEXT (src/db.js:1793) exi
- GET /api/corp/docs — Facility document register (leases/utilities/permits per building) is a corp-hub tool for org-wide staff; facility_docs.facility TEXT (src/db.js:1803) is presen
- GET /api/corp/payments — Payment-method reference vault (last4 only), corp-only, org-wide by design; payment_methods.facility TEXT (src/db.js:1944) available for optional filtering, fre
- GET /api/corp/insurance (+ /brokers, POST/DELETE, /settings, /run-reminders, /extract) — The coverage matrix intentionally crosses every entity × coverage type to flag org-wide gaps — scoping it to one facility would defeat its purpose. insurance_po
- GET /api/corp/leases, GET /api/corp/leases/:id, POST /api/corp/leases, POST /api/corp/leases/:id/ask, POST /api/corp/leases/extract, DELETE /api/corp/leases/:id — Corporate legal vault: all properties' leases for org-wide corp staff. leases.entity TEXT (src/db.js:1819) links to locations by name (used by landlord routing 
- GET /api/corp/entities (+ record/bank/card/portal writes, import, import-xlsx) — Entity vault is company-wide by design (legal/banking per legal entity, keyed by entity TEXT, no facility_id). For the record: despite the code comment 'masked'
- GET /api/corp/files/:id — Corp file blobs (insurance/lease PDFs); corp_files has no facility column (src/db.js:1636-1645) and files belong to org-wide corp records. Any corp user can fet
- GET /api/corp/intake (+ route/settings/send-instructions/test, POST /api/inbound/order webhook) — Email-order intake console is the org-wide corporate queue by design; the webhook (5237, token-gated, no session) stamps each created order with the routed enti
- GET /api/docs (+ POST/DELETE /api/docs, /api/docs/:id/read, /api/docs/:id/ack) — SOP/handbook library — docs table (src/db.js:500-505) has no facility column; company-wide policy content by design (the mission's named example). Read-tracking
- PATCH/DELETE /api/corp/tasks/:id, PATCH/DELETE /api/corp/orders/:id, POST /api/corp/orders/:id/email-landlord — Per-row mutations on the corporate work queues (lines 4991, 5003, 5097, 5129, 5090) have no facility guard — any corp user can work any location's item. That ma

## Gap: no facility linkage yet (schema queue)
- GET /api/meals/today (+ POST /api/meals/check, GET/POST /api/meals/snack, GET /api/meals/scorecard, GET/POST /api/meals/menu, GET /api/meals/feedback, GET /api/meals/insights) @ server.js:2589 — The whole meals world is single-house: meal_checks (src/db.js:924, UNIQUE(date,meal)), snack_checks (1412), meal_menu (962, UNIQUE(menu_date,meal)) have NO client_id and NO facility_id — two facilitie
- GET /api/laundry (+ POST /api/laundry, POST /api/laundry/:id/status, DELETE /api/laundry/:id) @ server.js:2656 — laundry_loads (src/db.js:1492) has no facility_id; client_id exists but is optional and never used for scoping (house loads have it NULL), so most rows cannot reach a facility. Not in FAC_SCOPED_API.
- GET /api/schedule (Program view; + POST /api/schedule, DELETE /api/schedule/:id) @ server.js:8739 — schedule_items (src/db.js:387) has no facility_id, and client_id NULL explicitly means 'facility-wide' (whole-house group/meal/activity) — so the join path only covers client-specific items; the house
- POST /api/belonging-pulse and GET /api/belonging-pulse/stats @ server.js:6225 — belonging_pulses (src/db.js:269) is deliberately anonymous (no client_id, no user_id) and has no facility_id, so leadership can never read staff belonging per facility. Stamping facility_id from facCt
- POST /api/referrals @ server.js:9634 — outbound_referrals.facility_id (src/db.js:797) is the DESTINATION partner (facilities table), not an org facility. Origin facility is only derivable when client_id is set; 'declined' referrals carry p
- POST /api/inbound-referrals @ server.js:9663 — inbound_referrals (src/db.js:811-819) has only the sending PARTNER's facility_id — no column, client link, or any other path saying which of the 11 org facilities received the referral. Reciprocity an
- POST /api/surveys/:id/respond @ /home/user/armada/server.js:11812 — Write stamps no facility. Named responses reach a facility via client_id -> clients.facility_id, but anonymous kiosk responses (client_id NULL) have NO path to any facility — they are permanently unat
- GET /api/beds (+ POST /api/beds) @ /home/user/armada/server.js:11066 — beds (src/db.js:477-482) has no facility_id. Occupied beds could be scoped via b.client_id -> clients.facility_id, but Open/Hold/Cleaning beds (client_id NULL) have no path to any facility — room/unit
- POST /api/beds/:id, POST /api/beds/:id/gender, DELETE /api/beds/:id @ /home/user/armada/server.js:11080 — Row-level bed operations cannot check facility access because beds has no facility_id — any user can mutate/delete any facility's bed row by id once the table is shared.
- POST /api/bedboard/total @ /home/user/armada/server.js:11120 — Total bed capacity is a single global app_state key ('detox_bed_count', read at 11091) — setting it from one facility changes every facility's bed board denominator. org_facilities already has a beds 
- GET /api/scan-points @ /home/user/armada/server.js:2933 — scan_points (src/db.js:2517-2528) has no facility_id — only a free-text room label. Every facility would share one QR-point list. Not in FAC_SCOPED_API.
- POST /api/scan-points (+ POST /api/scan-points/seed) @ /home/user/armada/server.js:2954 — Creates scan points with no facility stamp (no column exists). Worse, /seed (2969-2977) builds 'Room X' points from ALL facilities' client rooms — the clients query at 2970 is unscoped even though cli
- GET /api/scan-points/:id/qr.svg @ /home/user/armada/server.js:2982 — QR fetch by id; no facility check possible until scan_points is stamped (any admin can print any facility's codes).
- POST /api/round-scan @ /home/user/armada/server.js:3006 — round_scans (src/db.js:2529-2541) has no facility_id; a scan's facility is only implied by its scan_point, which is itself unstamped. The response also returns scanCoverage() (all facilities). The obs
- GET /api/rounds/coverage @ /home/user/armada/server.js:2915 — scanCoverage() computes overdue/covered across ALL scan points org-wide; no facility path (scan_points/round_scans unstamped). Not in FAC_SCOPED_API.
- GET /api/rounds/scorecard @ /home/user/armada/server.js:3036 — Per-employee scan accountability and the 'facility compliance %' mix every facility's scans and points — the compliance denominator (expected scans, 3042-3044) counts all points org-wide. No facility 
- GET /api/rounds/scan-review (+ GET /api/rounds/scan-photo/:id, POST /api/rounds/scan-flag) @ /home/user/armada/server.js:3053 — Scan-photo review lists/serves scans from all facilities (photo fetch at 3060, flag update at 3070 have no facility check). No facility path until scan_points/round_scans are stamped.
- GET+POST /api/rounds/escalation, GET+POST /api/rounds/room-hours, GET+POST /api/rounds/sweep-interval @ /home/user/armada/server.js:2897 — All rounds configuration is single-facility global state — one facility changing its sweep interval, room hours, or SMS escalation changes it for all 11. app_state has no facility dimension, so there 
- GET /api/billingready (lastRun/runs sub-parts) @ server.js:989 — The row list is correctly scoped (fc.frag('s.facility_id') at server.js:973), but billing_ready_runs (src/db.js:2183) has NO facility column, so with the chip set the 'last run'/run-history shown is w
- GET /api/finance/expenses @ server.js:11379 — Budget-vs-actual has NO facility dimension anywhere it stores data: budgets, actuals, categories, PPD lines, salaried roles and burden are single org-wide state blobs keyed only by month; staffing_sta
- GET /api/staffing-model (staffmodel view) + POST /standard + POST /log @ server.js:10259 — Neither staffing_standard nor shift_staffing has facility_id or any path to one (src/db.js:590-611; the only added column is 'rate', src/db.js:1289). There is ONE org-wide staffing standard, and shift
- GET /api/schedule/templates (weekgrid rows) + POST + DELETE @ server.js:10767 — shift_templates has no facility_id (src/db.js:988-996) — all 11 facilities share one set of weekly-grid shift rows; adding 'Resident Tech 7p-7a' for Hilltop puts it on every facility's grid, and delet
- GET /api/shift-checklist + POST /:id + POST /template/edit @ server.js:2676 — shift_tasks and shift_task_done have no facility_id (src/db.js:1502-1509) and UNIQUE(task_id, shift_date, shift) means one completion mark per shift ORG-WIDE — a tech at one building checking 'fridge 
- GET /api/shift-report + GET /history + POST @ server.js:2730 — shift_reports has no facility_id (src/db.js:1422-1432; only 'data' was added). Pass-downs from all facilities interleave: 'previous' report may be another building's, history mixes everyone, and — dat
- GET /api/rounds/coverage @ server.js:2915 — scan_points and round_scans have no facility_id (src/db.js:2517-2546 — only active_from/to, flagged, photo were added). The QR sweep coverage board is inherently one-building (detox) data presented or
- POST /api/shift-lead @ server.js:12419 — The lead-in-charge is one org-global app-state key with no facility dimension — setting the lead at one building changes who 'My Shift' shows as in charge at all 11.
- GET /api/command/checklist + POST /api/command/checklist/:id @ server.js:6538 — command_checklist (src/db.js:2586) has only date/section/item with UNIQUE(date, section, item) — exactly ONE Director's Daily Review instance per day for the whole org. Eleven facility directors would
- GET /api/opscenter — beds tile @ server.js:432 — beds (src/db.js:477) has room/label/unit/status/client_id but no facility_id — an OPEN bed cannot be attributed to any facility (occupied ones could go via client_id, open ones cannot). The tile hones
- GET /api/command/overview — daily trend block @ src/db.js:2398 — daily_metrics is keyed on date alone — one org-wide row per day; rollupDailyMetrics counts flow_events and census with no facility dimension, so the 14-day intake/discharge/AMA trend in command/overvi
- GET /api/meals/scorecard @ server.js:2652 — meal_checks (src/db.js:924, UNIQUE(date,meal)) has no facility column — one kitchen log per day org-wide; single-detox-kitchen legacy. No path to a facility. Scorecard-family surface included for comp
- GET /api/rounds/scorecard @ server.js:3031 — QR sweep-rounds compliance: round_scans/scan_points (src/db.js:2517-2546) carry no facility column and no client link — physical scan points are implicitly the detox building. Note the plain /api/roun
- GET /api/turnovers/scorecard @ server.js:10686 — bed_turnovers (src/db.js:211) stores only a room TEXT — no facility_id, no client_id, and beds itself has no facility column, so room strings can't be mapped to a facility. No linkage path exists toda
- POST /api/lineup-log @ server.js:12674 — A lineup huddle happens per facility, but lineup_log has no facility_id and UNIQUE(date,shift) means once Detox Akron logs the Day lineup, every other facility's log for the same date+shift is silentl
- GET/POST/PATCH/DELETE /api/hr/employees (5681/5693/5700/5710) @ server.js:5682 — The salary roster groups by hr_employees.entity — a free-text company name (src/db.js:2321-2334). There is no facility_id and no FK/mapping from entity to org_facilities, so ?facility= can never filte
- HCOS reads: GET /api/hcos/overview (5726), /people (5747), /person/:id (5751), /cases (5861), /leave (5882), /reqs (6011), /analytics (6066), POST /api/hcos/ask (5886) @ server.js:5726 — Every HCOS table (hr_reviews, hr_certifications, hr_coaching, hr_cases, hr_leave, hr_onboard_tasks, hr_events, hr_requisitions, hr_comp_history) keys to hr_employees.employee_id, and hr_employees has 
- HCOS writes: POST /api/hcos/person/:id (5766), start-onboarding (5776), onboard/task/:id/toggle (5799), cert (5806), review (5816/5822), coaching (5831/5841), case (5845/5854), leave (5864/5874), start/complete-offboarding (5959/5974), reqs (6004/6014), POST /api/myhr/leave (6046) @ server.js:5776 — All HR writes stamp rows keyed only by employee_id (or free-text entity for requisitions). No facility is stamped and none can be derived, because hr_employees has no facility_id. Once the hr_employee
- GET /api/onboarding (6413), POST /api/onboarding (6427), POST /api/onboarding/:id/task (6434), DELETE /api/onboarding/:id (6442) @ server.js:6414 — The 'sacred onboarding' (Day 1 / Day 21) tracker stores the new hire as a free-text name — no user_id, no hr_employees id, no facility_id. Leadership at any facility sees every facility's new hires an
- POST /api/kiosk/survey @ server.js:12315 — survey_responses has NO facility_id column (src/db.js:356-362). Named responses reach a facility via client_id→clients.facility_id, but client_id is optional and anonymous responses have no path to an
- POST /api/kiosk/meal @ server.js:12342 — meal_feedback has no facility_id (src/db.js:946-957); client_id is optional, so anonymous meal pulses can never be attributed to a building even by join, and the known device fid is discarded. Compoun
- POST /api/sl-kiosk/suggestion (+ checkin :1371, request :1380, survey :1397, activity-feedback :1410) @ src/housing.js:1390 — None of the five SL-kiosk write tables (housing_checkins, housing_requests, housing_suggestions, housing_survey_responses, housing_activity_feedback) carries facility_id; reads reach a facility only t
- GET /api/housing/orders @ src/housing.js:1858 — housing_orders (schema 355-360: vendor,status,total,note,by,ordered_at,received_at) has no house_id or facility_id, so a facility pick cannot filter purchase orders at all. The only indirect path is h
- GET /api/sl-kiosk/data @ src/housing.js:1359 — The resident kiosk returns the full org-wide active-resident roster (names + houses) to any device holding the ONE shared kiosk code (slKioskCode(), line 785 — a single global state key 'sl_kiosk_code
- GET /api/sl-kiosk/activities @ src/housing.js:1405 — Same kiosk-identity gap as /api/sl-kiosk/data: residents rating activities see the last 3 days of events from EVERY facility (events do carry house_id -> facility, so the data side is linkable; the re
- GET /api/housing/lineup (+ POST /api/housing/lineup) @ src/housing.js:2573 — housing_lineup (schema 401-405: date, standard_index, led_by, attendees, wow_story, focus_note) has NO house_id or facility_id, so the daily line-up ritual is recorded as one org-wide stream: today's 
- GET/POST /api/housing/shift-tasks @ src/housing.js:2450 — Per-house buckets are fine (house_id -> housing_houses.facility_id), but the default houseId=0 'all houses' bucket (house_id NULL, insert at 2465 stores NULL) is a single org-wide checklist: a Dayton 
- GET /api/housing/orders @ src/housing.js:1858 — housing_orders has NO house_id or facility_id (schema src/housing.js:355-360: vendor,status,total,note,by,ordered_at,received_at). The only path to a facility is housing_order_lines.item_id → housing_
- POST /api/housing/orders/suggest @ src/housing.js:1865 — Suggested orders are built from ALL houses' low stock org-wide and the created order rows carry no facility linkage, so a facility-scoped operator generates and sees other facilities' reorders.
- POST /api/projects (+ /api/projects/:id/checklist, DELETE /api/projects/:id) @ server.js:10539 — projects table (src/db.js:681-688) has no facility column and no join path to one — the DOO project board is a single org-wide list. Any facility's operator sees and edits every project via /api/ops. 
- GET /api/turnovers @ server.js:10683 — Bed-turnover board (route at 10681; only /turnovers/scorecard was in the slice list) reads bed_turnovers, which carries only a room TEXT and the client's first name (src/db.js:211) — no facility path.
- GET /api/ops @ server.js:10470 — The whole Operations hub is single-building: environment_checks and ops_handoffs are UNIQUE(date,shift) with no facility column (src/db.js:653, 663), so two facilities logging the same shift overwrite
- POST /api/crisis-owner @ server.js:12726 — The shifts table keys one row per (date, shift-name) for the whole company, so naming a Crisis Owner sets it org-wide — 11 facilities cannot each name their own. Same org-global shifts row underlies c
